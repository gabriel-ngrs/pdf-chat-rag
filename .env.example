# Chave da API do Google Gemini (LLM + embeddings).
# Obtenha em https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Postgres — os defaults abaixo funcionam para rodar localmente via docker compose.
POSTGRES_USER=talkdoc
POSTGRES_PASSWORD=talkdoc
POSTGRES_DB=talkdoc
